name: Template terraform code and create PR
on:
  workflow_dispatch:
    inputs:
      productName:
        description: 'Product Name'
        required: true
        default: 'airslate'
      repoName:
        description: 'Repo name'
        required: true
        default: 'blabla'
jobs:
  create-pr:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "Product: ${{ github.event.inputs.productName }}"
        echo "Repo: ${{ github.event.inputs.repoName }}"
